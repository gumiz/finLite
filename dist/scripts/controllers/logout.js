'use strict';
angular.module('finLiteApp').controller('LogoutCtrl', ['$scope', 'ajaxService', 'notify', '$location', function ($scope, ajaxService, notify, $location) {
  //debugger;
  //  ajaxService.doGet('logout').then(function(){
  //    $location.path('/');
  //  });
  //$scope.login = function() {
  //  debugger;
  //    var params = {username: $scope.user.name, password: $scope.user.pass};
  //    ajaxService.doPost('login', params);
  //  };
}]);
